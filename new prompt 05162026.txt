You are working on my Django 6 IVS project.

Goal:
Refactor volunteer attendance and assignment logic toward a term-based volunteer profile model, and add AJAX hard-delete for volunteers so the page does not refresh.

Important context:
Currently the system has:
- Volunteer
- VolunteerTeamAssignment
- DutyAssignment
- attendance/status fields connected through duty assignments
- Volunteer hard delete currently fails because DutyAssignment protects VolunteerTeamAssignment through protected FK.

New design I want:
Create or introduce a model named VolunteerTermProfile or similar.

Purpose:
One volunteer should have one term/jamatkhana profile per term and JK/local council context. Team assignment, appointment, duty assignment, and attendance should use this profile as the central person-for-this-term record.

Concept:
Volunteer = permanent biodata/person record.
VolunteerTermProfile = volunteer’s active record for one term/JK/LC.
VolunteerTeamAssignment = assignment of that term-profile to one team.
Appointment = appointment for that term-profile.
DutyAssignment = duty assignment for that term-profile or for a team assignment if needed.
Attendance = should belong to VolunteerTermProfile + DutyPlan/PlanDate/Session/Slot logic, not only to a specific team assignment.

Main requirement:
If one volunteer is assigned multiple times in the same day/session/plan through different teams or duties, attendance should only be recorded once for that VolunteerTermProfile for that duty day/session/plan. It should not matter whether the volunteer appears under x team or y team.

Please inspect the existing models, views, forms, templates, and admin logic before changing anything.

Tasks:

1. Add VolunteerTermProfile model
Create a model like:

- volunteer FK to Volunteer
- term FK to Term
- jk FK to Jamatkhana if needed
- local_council FK if needed
- is_active boolean
- notes optional
- created_at / updated_at

Add a unique constraint so one volunteer cannot have duplicate profile rows for the same term + JK/LC scope.

Use the existing project naming conventions and scoped manager patterns.

2. Update related models carefully
Inspect these models first:
- VolunteerTeamAssignment
- Appointment
- DutyAssignment
- DutyPlan
- DutySlot
- attendance-related fields/models

Add FK to VolunteerTermProfile where appropriate.

Do not remove the old volunteer FK immediately unless it is safe. Prefer a phased migration:
- Add nullable VolunteerTermProfile FK first
- Backfill data migration
- Update views/forms/templates to use VolunteerTermProfile
- Keep old fields temporarily for backward compatibility
- Only suggest removal after confirming all references are migrated

3. Attendance redesign
Create or update attendance logic so attendance is unique per volunteer term profile for the relevant duty occurrence.

Desired behavior:
If Volunteer A appears in multiple DutyAssignment rows for the same plan/date/session, marking attendance once should apply to that volunteer’s attendance for that occurrence.

Add/adjust model constraints if needed, for example:
VolunteerTermProfile + DutyPlan/RosterDay + session/frequency/date/slot combination should be unique.

Use existing attendance status choices:
- Present
- Absent
- Late
- Excused
- Unmarked
or whatever is already defined in the project.

4. Backfill migration
Create a safe migration or management command that:
- Finds existing VolunteerTeamAssignment records
- Creates VolunteerTermProfile for each volunteer + term + JK/LC context
- Links VolunteerTeamAssignment to VolunteerTermProfile
- Links DutyAssignment to VolunteerTermProfile if the field is added
- Preserves existing attendance/status data

Do not delete data.

5. Update forms/views/templates
Update roster, duty assignment, attendance, team assignment, appointment, exports, WhatsApp message generation, filters, and reports only where required.

Important:
Do not break existing pages.
Preserve current UI, JS, filters, exports, and WhatsApp logic unless required for this refactor.





9. Deliverables
Give me:
- exact model changes
- migration files or commands
- updated view function for volunteer_hard_delete
- updated template JS for AJAX delete
- any admin/form updates required
- explanation of any risky parts
- test checklist

Do not make broad unrelated changes.
Do not rename existing URLs unless absolutely required.
Keep backward compatibility where possible.