{% extends "base.html" %}
{% block title %}WhatsApp Role Simulator{% endblock %}
{% block content %}
<div class="container-fluid px-0" style="max-width:1100px;">
  <div class="d-flex justify-content-between align-items-start flex-wrap gap-2 mb-3">
    <div>
      <h3 class="mb-1"><i class="bi bi-phone me-2"></i>WhatsApp Role Simulator</h3>
      <div class="text-muted small">Test tenant, staff, or handyman behaviour without changing registered phone numbers.</div>
    </div>
    <a href="{% url 'whatsapp:webhook_log_list' %}" class="btn btn-outline-secondary btn-sm">WhatsApp Logs</a>
  </div>

  <div class="alert alert-warning py-2 small">
    Simulator actions use real local data and may create pending approval records. Phone delivery is optional and is clearly labelled as a simulation.
  </div>

  <div class="row g-3">
    <div class="col-12 col-lg-7">
      <form method="post" enctype="multipart/form-data" class="card shadow-sm">
        {% csrf_token %}
        <div class="card-header py-2"><strong>Simulated inbound message</strong></div>
        <div class="card-body">
          {% if form.non_field_errors %}<div class="alert alert-danger py-2">{{ form.non_field_errors }}</div>{% endif %}
          <div class="row g-2">
            <div class="col-12 col-md-4"><label class="form-label small mb-1">Role</label>{{ form.role }}{{ form.role.errors }}</div>
            <div class="col-12 col-md-8"><label class="form-label small mb-1">Tenant</label>{{ form.tenant }}{{ form.tenant.errors }}</div>
            <div class="col-12"><label class="form-label small mb-1">Active lease (Tenant Mode)</label>{{ form.lease }}{{ form.lease.errors }}</div>
            <div class="col-12 col-md-6"><label class="form-label small mb-1">Staff</label>{{ form.staff }}{{ form.staff.errors }}</div>
            <div class="col-12 col-md-6"><label class="form-label small mb-1">Handyman</label>{{ form.handyman }}{{ form.handyman.errors }}</div>
            <div class="col-12"><label class="form-label small mb-1">Message / media caption</label>{{ form.message }}{{ form.message.errors }}</div>
            <div class="col-12"><label class="form-label small mb-1">Optional media</label>{{ form.media }}{{ form.media.errors }}</div>
            <div class="col-12 col-md-6"><label class="form-label small mb-1">Deliver response to</label>{{ form.delivery_phone }}{{ form.delivery_phone.errors }}</div>
            <div class="col-12 col-md-6 d-flex flex-column justify-content-end gap-1 pb-1">
              <div class="form-check">{{ form.send_to_phone }} <label class="form-check-label" for="{{ form.send_to_phone.id_for_label }}">{{ form.send_to_phone.label }}</label></div>
              <div class="form-check">{{ form.new_session }} <label class="form-check-label" for="{{ form.new_session.id_for_label }}">{{ form.new_session.label }}</label></div>
            </div>
          </div>
        </div>
        <div class="card-footer py-2 text-end"><button class="btn btn-success btn-sm" type="submit"><i class="bi bi-send me-1"></i>Simulate Message</button></div>
      </form>
    </div>
    <div class="col-12 col-lg-5">
      <div class="card shadow-sm h-100">
        <div class="card-header py-2"><strong>Assistant response</strong></div>
        <div class="card-body">
          {% for response in responses %}
            <div class="border rounded bg-light p-3 mb-2" style="white-space:pre-wrap;">{{ response }}</div>
          {% empty %}
            <div class="text-muted small">Submit a simulated message to see the response here.</div>
          {% endfor %}
          {% if simulator_conversation %}<div class="text-muted small mt-3">Conversation: {{ simulator_conversation.phone_number }} · State: {{ simulator_conversation.pending_state|default:"none" }}</div>{% endif %}
        </div>
      </div>
    </div>
  </div>
</div>
{% endblock %}
